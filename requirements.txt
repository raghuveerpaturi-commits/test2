# Core dependencies for face detection on Jetson Nano
opencv-python>=4.5.0
numpy>=1.19.0

# Optional: Face recognition (requires dlib compilation on Jetson)
# Uncomment if you want to use --recognize feature
# face-recognition>=1.3.0
# dlib>=19.22.0

# For DNN model download (optional)
wget>=3.2
